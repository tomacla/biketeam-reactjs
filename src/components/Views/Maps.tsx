import React, { FC, memo } from 'react';
import { ViewContainer } from './common';

const Maps: FC = () => {
  return (
    <ViewContainer>
      TODO: Maps
    </ViewContainer>)
}

export default memo(Maps);

import React, { FC, memo } from 'react';
import { ViewContainer } from './common';

const Map: FC = () => {
  return (
    <ViewContainer>
      TODO: Map
    </ViewContainer>)
}

export default memo(Map);

import React, { FC, memo } from 'react';
import { ViewContainer } from './common';

const Rides: FC = () => {
  return (
    <ViewContainer>
      TODO: Rides
    </ViewContainer>)
}

export default memo(Rides);

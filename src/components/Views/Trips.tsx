import React, { FC, memo } from 'react';
import { ViewContainer } from './common';

const Trips: FC = () => {
  return (
    <ViewContainer>
      TODO: Trips
    </ViewContainer>)
}

export default memo(Trips);

import React, { FC, memo } from 'react';
import { ViewContainer } from './common';

const Ride: FC = () => {
  return (
    <ViewContainer>
      TODO: Ride
    </ViewContainer>)
}

export default memo(Ride);
